2026-03-17

Статус:
- Начат первый этап: веб-форма по образцу `nalog docs/пример заполнения.xlsx`.

Что уже сделано:
- Репозиторий скачан.
- Изучены: `задание.txt`, Excel-шаблон и сопутствующие markdown-файлы.
- Создан первый прототип веб-интерфейса:
  - общая вкладка с реквизитами и шапкой;
  - отдельные вкладки для листов КС-2;
  - вкладка КС-3;
  - вкладка `Удержания`;
  - вкладка `Дополнительные поля для XML`.
- Реализовано добавление новых листов КС-2.
- Реализовано построчное редактирование строк работ, разделов и примечаний.
- Реализованы базовые расчёты по строкам и итогам.
- Подготовлены демо-данные из Excel в `data/sample-data.json`.
- Добавлен локальный запуск через `./scripts/run_local.sh`.

Что пока не сделано:
- Генерация Excel в формате исходного шаблона.
- Генерация XSD-валидного XML.
- Полный перенос всех формул Excel один в один.
- Финальная бизнес-валидация обязательных полей.

Дополнительно сделано:
- Собраны 3 визуальных варианта формы для сравнения:
  - Excel-like;
  - Modern Light;
  - Dark Dashboard.
- Каждый вариант запущен локально на отдельном порту для выбора оформления.

Дополнительно после выбора варианта 4174:
- Вариант `Modern Light` выбран как основной для дальнейшей доработки.
- Уменьшены размеры строчных полей в табличной части.
- Добавлено переключение плотности строк (`Компактные строки`).
- Добавлено управление масштабом интерфейса для более удобного просмотра длинных таблиц.
- В таблице КС-2 расширена колонка `Наименование`, поджаты узкие числовые колонки.
- Во вкладке `Удержания` уменьшена колонка `% удержания` и расширена колонка наименования.
- Кнопки удаления строк заменены на компактные крестики.
- Для строк КС-2 сделан компактный блок действий: сверху зелёный плюс, снизу крестик удаления.
- Добавлено всплывающее меню вставки строки (`раздел` / `строка` / `примечание`).
- Добавлено подтверждение удаления строки с кнопками `✓` и `×`.
- Добавлено скрытие/показ левого меню по клику, с растягиванием формы на доступную ширину.
- Уменьшены внешние отступы слева и справа.
- Добавлена липкая шапка таблиц.
- Добавлен ручной resize колонок мышкой для КС-2 и удержаний.
- Ширина колонок сохраняется и не сбрасывается, в том числе отдельно по разным листам КС-2.
- Ручная высота текстовых полей сохраняется и не сбрасывается после перерисовки формы.
- Столбец действий дополнительно сужен под фактический размер блока `+ / ×`.
- Добавлена липкая шапка таблиц.
- Добавлен ручной resize колонок мышкой в ключевых таблицах.
- Для строк КС-2 добавлен компактный блок действий: сверху `+`, снизу `×`.
- Для удаления строки КС-2 добавлено подтверждение через всплывающее окно с галочкой и крестиком.

Дополнительно по этапу 1:
- Начато приведение интерфейса к структуре исходного Excel.
- Для КС-2 и КС-3 добавлены шапки, визуально ближе к унифицированным формам.
- Для ключевых таблиц добавлены нумерационные строки колонок в стиле Excel.
- Для вкладки `Удержания` добавлен верхний блок, повторяющий смысл исходного расчётного листа.
- Повторяющиеся шапки и подписи централизованы через общие реквизиты.
- Переключатели показа шапок и подписей вынесены из `Реквизитов` в верхнюю панель рядом с управлением меню.
- Подписи КС-2 и КС-3 теперь берутся из общих реквизитов, а не настраиваются по отдельности в каждой вкладке.

Дополнительно по этапу 1:
- Начато приближение нижних блоков документов к Excel-образцу.
- Для КС-2 добавлен отдельный итоговый блок и отдельный набор подписей `Сдал / Принял / Проверил`.
- Для КС-3 добавлен отдельный итоговый блок и отдельные подписи `Застройщик / Технический Заказчик / Генподрядчик`.
- Для `Удержаний` добавлен отдельный итоговый блок и подписи в том же составе, что в Excel.
- Параметры подписантов вынесены в `Реквизиты`.
- Низы документов переведены с карточного отображения на более табличный формат, ближе к Excel-шаблону.
- Подписные блоки перестроены в трёхстрочную схему, ближе к реальной структуре Excel-листов.
- Итоговые строки КС-3 и удержаний уточнены по формулировкам и структуре отображения.
- Итоговый блок `Удержаний` расширен ближе к фактическим итоговым колонкам исходного Excel.
- Низ КС-2 получил отдельный правый блок расшифровки (`материал / металлопрокат / бетон / пр.мат. / работа`) как в Excel.
- Подписи переведены в более буквальную excel-структуру: основная строка, строка пояснений, строка `М.П.`.
- В `Реквизиты` добавлены недостающие поля структуры: `ОКПО объекта`, `ОКДП`, отдельные заголовки и подзаголовки документов КС-2 / КС-3.
- Шапки КС-2 и КС-3 теперь используют эти поля и стали буквальнее по отношению к Excel.
- Вкладка `Удержания` дополнительно приближена к Excel-структуре.
- Для строк `Удержаний` добавлены такие же компактные действия, как в КС-2: `+` и `×`.
- В меню `+` для `Удержаний` добавлены варианты: `Добавить раздел ниже` и `Добавить подпункт внутри раздела`.
- Подпункт в `Удержаниях` рендерится как вложенная строка с активными колонками 4–8, как в логике Excel-листа.

Структурный этап — текущее состояние:
- Основные вкладки и шапки уже собраны.
- Верхние блоки КС-2 / КС-3 / Удержаний приближены к Excel.
- Нижние блоки КС-2 / КС-3 / Удержаний приближены к Excel.
- В `Реквизитах` собраны общие поля, подписанты, ОКПО объекта, ОКДП, заголовки и подзаголовки документов.
- Интерфейс для ручной работы уже стабилен: листы КС-2, строки, действия, ресайз, скрытие меню, переключение шапок/подписей.

Логический этап — начало:
- Добавлена нормализованная модель документов поверх текущего UI-состояния.
- Добавлена первичная проверка обязательных полей и предупреждений.
- В `Реквизитах` появился блок логики формы с количеством ошибок/предупреждений и списком проблем.
- Экспорт JSON теперь включает не только состояние формы, но и логическую модель + результат проверки.
- Для вкладки `Удержания` подтверждена и зафиксирована логика Excel:
  - у подпункта `F` и `G` вводятся вручную;
  - `H` считается автоматически как `F - G`;
  - для строки раздела `I = B * % удержания`;
  - для строки раздела `J = B - ΣG подпунктов - I`.
- Для строки раздела `Удержаний` колонки `D/F/G/H` переведены на агрегаты по подпунктам.
- Подготовлен первичный логический маппинг `Удержаний` в XML-модель расчётов:
  - зачет аванса → `ВидУдерж = 31`;
  - гарантийное удержание → `ВидУдерж = 32`.

Дополнительно после начала XML-этапа:
- Добавлен скрипт `scripts/generate_xml_from_export.py` для сборки XML 1110335 из экспортированного JSON модели формы.
- Добавлен скрипт `scripts/validate_xml_xsd.py` для проверки XML по схеме `nalog docs/ON_AKTREZRABP_1_971_01_01_00_03.xsd`.
- XML-генерация построена поверх рабочего PASS-шаблона и заполняет ключевые блоки:
  - `Файл` / `Документ`;
  - `СвАктСдПр`;
  - `НаимИСт`;
  - `СвОРасч`;
  - `ВсегоАктОтч`;
  - `НастрФормДок`;
  - `ПодписантПодр`.
- Подготовительный XML-мэппинг удержаний встроен в экспортную модель и используется генератором.
- Выполнен тестовый прогон: генерация XML из экспортной модели и успешная XSD-валидация (`VALID`).
- В `modern-light` добавлена кнопка `Экспорт XML (XSD-ready)`.
- Локальный запуск переведен с простого `http.server` на `scripts/run_local_server.py`.
- Добавлен API-маршрут `POST /api/export-xml`, который:
  - принимает текущую экспортную модель формы;
  - собирает XML;
  - валидирует XML по XSD;
  - отдает XML на скачивание только если проверка пройдена.
- Если XSD-проверка не пройдена, браузерный экспорт XML не выполняется и пользователю показываются ошибки.
- Добавлен единый сценарий `scripts/export_and_validate_xml.sh`, который:
  - берет экспортированный JSON формы;
  - собирает XML;
  - сразу прогоняет его через XSD-валидацию.

Дополнительно по safe Diadoc/XML профилю:
- Добавлена предэкспортная серверная проверка обязательных XML-реквизитов до XSD-валидации.
- Если не заполнены критичные поля (`ИНН`, составитель XML, версия сметы, реквизиты допсоглашения, индекс/регион, ФИО подписанта и др.), XML не отдается на скачивание.
- Ошибки незаполненных полей возвращаются из API и показываются в интерфейсе вместо скачивания файла.
- В Python-генераторе включены safe-дефолты для служебных полей (`ВерсПрог`, `ВерсФорм`) и фильтрация пустых разделов/нулевых `СвВидРаб`.

Дополнительно на compact sample:
- Стартовый demo-образец заменен на компактный кейс `small sample.xlsx` (1 лист КС-2, 1 лист КС-3, компактные удержания).
- Добавлен server save `saved-forms/small-sample.json`.
- На вкладке `Удержания` активирована колонка комментариев для подпунктов, значения перенесены из Excel.
- Для compact XML-профиля `СвОРасч` переведен в pass-friendly режим: строки `УчетТребУдерж` агрегируются в одну запись, чтобы соответствовать XSD/проходному профилю.
- Тестовый компактный XML (`output/small-sample.xml`) снова проходит XSD-валидацию (`VALID`).

Дополнительно по сближению с минимальным pass-profile:
- Для compact sample включен `pass-friendly` режим `НаимИСт`: экспортируется компактная структура, ближе к `sample_1110335.xml` (один `ВидРаб` + один `Раздел`), при этом исходные бизнес-данные сохраняются в форме и модели.
- Исправлен источник подписанта XML: `ПодписантПодр` теперь формируется из `xmlExtras.manual.signerName` / подписи small sample, а не из fallback.
- После этих изменений compact XML остается XSD-valid (`output/small-sample.xml` = `VALID`).

Дополнительно по `СвАктСдПр` и служебным полям:
- В compact XML-профиле `СвЗак` для pass-friendly сценария переведен на техзаказчика (`techCustomerName`), что сближает экспорт с `sample_1110335.xml`.
- `ИнфПолФХЖ1/ТекстИнф/@Значение` переведен на sample-friendly default `sample` (с возможностью переопределения через manual-поле).
- После этих точечных изменений compact XML по-прежнему проходит XSD-валидацию (`VALID`).

Дополнительно по production-подходу:
- На основании `nalog target.docx` и XSD подтверждено, что агрегировать табличные части не требуется: формат допускает множественные `ВидРаб`, множественные `Раздел`, а также `СвВидРаб` внутри разделов.
- Compact режим оставлен только как fallback/debug profile; для production-логики принят курс на `full / как в форме`.
- В XML-вкладку добавлен явный переключатель режима табличной части (`compact` / `full`).
- Добавлены richer XML-поля следующего уровня: `signerStatus`, `signatureType`, `customInfoValue`.
- Для compact sample и server save дефолт режима переключен на `full / как в форме`.
- Создана отдельная сводка `nalog docs/табличные части production XML.md` с production-выводами по табличным частям и масштабированию на несколько КС-2.

Дополнительно по production-full табличной части:
- Python-генератор переведен на production-full режим табличной части:
  - `Раздел` формируются из section-строк КС-2;
  - `СвВидРаб` формируются из item-строк внутри разделов;
  - введена сквозная нумерация `НомСтр` и `НомРазд`;
  - в строки работ передаются `НомПоз`, `ПозСмет`, `ЦенаТов`, `СтТовБезНДС`, `КолТов`, `ОКЕИ_Стройка`, `НаимЕдИзм`, `СумНал`.
- Для удержаний в `СвОРасч` пока сохранен агрегированный профиль, так как текущая XSD-конфигурация ожидает единственный `УчетТребУдерж`.
- Full-profile на compact sample подтвержден: `output/small-sample-full.xml` проходит XSD-валидацию (`VALID`).

Дополнительно по richer production-полям разделов:
- В full-profile для `Раздел` добавлены `ПозРаздСмет` и `СтСНДСРаздОтч`.
- `ПозРаздСмет` сейчас безопасно вычисляется из первого `estimateNo` внутри раздела.
- `СтСНДСРаздОтч` вычисляется как `СтБезНДСРаздОтч + НДС` по ставке документа.
- После добавления richer полей разделов production-full XML по-прежнему проходит XSD (`output/small-sample-full.xml = VALID`).

Дополнительно по richer production-полям строк работ:
- В `СвВидРаб` добавлены safe-вычислимые richer атрибуты `СтПоСметеБезНДС` и `СтТовУчНал`.
- Проверено эмпирически, что `КолТов` должен передаваться как дочерний элемент, а не атрибут.
- Проверено эмпирически, что `НалСтПостр` в текущем XSD-профиле нельзя вставлять как дочерний элемент `СвВидРаб`; это поле пока исключено из XML-генерации.
- После добавления richer полей строк production-full XML по-прежнему проходит XSD (`output/small-sample-full.xml = VALID`).

Дополнительно по richer суммам разделов:
- В full-profile для `Раздел` добавлены также `СтБезНДСРаздСмет` и `СтСНДСРаздСмет`.
- Для текущего ненакопительного production-кейса они безопасно приравниваются к отчетным суммам раздела.
- После добавления richer сумм разделов production-full XML по-прежнему проходит XSD (`output/small-sample-full.xml = VALID`).

Дополнительно зафиксированы решения по передаче данных из веб-формы в production XML:
- Подписи со стороны заказчика и техзаказчика нужно передавать в XML.
- Текстовые ярлыки подписей (`Сдал`, `Принял`, `Проверил`) в XML не передаются.
- OKПО участников нужно передавать в XML.
- Реквизиты шапки формы считаются значимыми и должны максимально передаваться в XML.
- Валюта в человекочитаемом виде отдельно не требуется: достаточно кода.
- В `ПодписантПодр` нужно передавать не только ФИО, но и `Должн`, `СтатПодп`, `ТипПодпис`.
- Почтовый индекс и регион подрядчика нужно передавать в XML.
- Комментарии из формы / удержаний в XML не передаются.
- Полный набор подписей из листа удержаний нужно отразить в XML минимум один раз в допустимых блоках формата.

Дополнительно подготовлена точная карта `поле формы -> XML узел`:
- `nalog docs/карта полей формы в XML.md`
- В карте отдельно зафиксированы: минимальный production-базис из `docx`, уже внедренные поля генератора, данные, уже заполненные в веб-форме, и оставшийся обязательный хвост внедрения (`ПодписантПодр`, подписи сторон, OKПО, адрес подрядчика, реквизиты шапки).

Дополнительно по richer `ПодписантПодр`:
- В `ПодписантПодр/Подписант` внедрены `СтатПодп`, `ТипПодпис`, `Должн`.
- Значения берутся из `xmlExtras.manual.signerStatus`, `signatureType`, `signerPosition` (с fallback на подпись подрядчика из формы).
- После внедрения richer атрибутов `ПодписантПодр` production-full XML по-прежнему проходит XSD (`output/small-sample-full.xml = VALID`).

Дополнительно по OKПО и адресу подрядчика:
- В `СвПодр/СвСторДог` и `СвЗак/СвСторДог` внедрен `ОКПО`.
- Для подрядчика в XML добавлен штатный блок `Адрес/АдрРФ` с `Индекс` и `КодРегион`.
- После внедрения OKПО и адреса подрядчика production-full XML по-прежнему проходит XSD (`output/small-sample-full.xml = VALID`).

Дополнительно по подписям сторон:
- По `nalog target.docx` / XSD подтверждено, что текущий XML — это файл обмена информации подрядчика.
- В нем штатно предусмотрен `ПодписантПодр`, но не предусмотрены отдельные подписанты заказчика/техзаказчика.
- Для текущего подрядческого XML считаем достаточным передавать заказчика/техзаказчика как стороны договора; их подписи отдельно в этот XML не встраиваем.

Дополнительно по реквизитам шапки и сторонам договора:
- В `СвПодр/СвСторДог` и `СвЗак/СвСторДог` добавлены `ИнфДляУчаст` и `КраткНазв`.
- В `СвАктСдПр` добавлен `ОснСдачи` на основе основания листа КС-2 (в корректной позиции по XSD-порядку элементов).
- После добавления этих реквизитов production-full XML по-прежнему проходит XSD (`output/small-sample-full.xml = VALID`).
- Отдельно отмечено: `ИдСмет` в текущем шаблоне не присутствует как готовый узел, его нужно будет создавать явно, если решим передавать его не только через `ИзмСмет`.

Дополнительно по реквизитам шапки:
- `ИдСмет` теперь создается явно как отдельный XML-узел и заполняется типом документа, номером и датой.
- После явного создания `ИдСмет` production-full XML по-прежнему проходит XSD (`output/small-sample-full.xml = VALID`).

Ключевая веха:
- Файл `output/small-sample-full.xml` прошел внешнюю проверку в Контуре без замечаний.
- Это подтверждает, что текущий production-full XML-профиль small sample соответствует рабочим ожиданиям внешнего валидатора, а не только локальной XSD-схеме.
- Дополнительно базовый файл заказчика `output/small-sample-customer.xml` доведен до XSD-valid состояния по `ON_AKTREZRABZ.xsd`.
- На small sample фактически собран рабочий комплект из двух XML-файлов: `P` (подрядчик / КС-2-профиль) и `Z` (заказчик / КС-3-профиль).
- Итоговая сводка по текущей готовности комплекта зафиксирована в `nalog docs/сводка по готовности комплекта P+Z.md`.

Следующий шаг:
- Распространять этот production-full подход на более крупные/реальные наборы данных (несколько КС-2, более сложные удержания), сохраняя ту же структуру XML.

Дополнительно по удержаниям:
- В `СвОРасч/УчетТребУдерж` добавлен `ДокПодтСумУд`.
- Для текущего агрегированного XSD-профиля документ берется из первой релевантной строки удержаний, где заполнен документ-основание.
- После добавления `ДокПодтСумУд` production-full XML по-прежнему проходит XSD (`output/small-sample-full.xml = VALID`).

Дополнительно по комплекту XML-файлов:
- В каталоге `XSD__DOCS_FORMS_37783-KS-3` подтверждено наличие двух отдельных XSD-схем:
  - `ON_AKTREZRABP.xsd` — файл информации подрядчика;
  - `ON_AKTREZRABZ.xsd` — файл информации заказчика.
- Дополнительно проверено, что папки `XSD__DOCS_FORMS_37783-KS-3` и `XSD__DOCS_FORMS_37782-KS-2` содержат одни и те же XSD-файлы: схемы в них побайтно совпадают.
- После повторного чтения `nalog target.docx` и сверки со страницами структуры Контура зафиксировано уточнение: комплект состоит из **2 файлов**, а не из 3.
- По структурам Контура подтверждено соответствие:
  - `КС-2` -> `ON_AKTREZRABP` (файл подрядчика);
  - `КС-3` -> `ON_AKTREZRABZ` (файл заказчика).
- Текущий production-full генератор соответствует подрядческому файлу (`...P`) и находится уже в почти готовом состоянии.
- Подписи заказчика в текущий XML подрядчика отдельно не встраиваются не потому, что они не нужны, а потому что для них предусмотрен отдельный файл заказчика (`...Z`).
- Следующий архитектурный шаг: доводить комплект из двух файлов:
  1. XML подрядчика / КС-2 (`...P`);
  2. XML заказчика / КС-3 (`...Z`).

Дополнительно по XML заказчика (`...Z`):
- Добавлен отдельный генератор `scripts/generate_customer_xml_from_export.py`.
- В `...Z` реализованы и проходят XSD базовые блоки:
  - `ОснДоверОргСост`
  - `ИдИнфПодр`
  - `СодФХЖ4`
  - `ПодписантЗак`
- Источником подписанта заказчика сейчас принят `customerSignerName/customerSignerPosition` с fallback на техзаказчика.
- Файл `output/small-sample-customer.xml` проходит XSD-валидацию по `ON_AKTREZRABZ.xsd` (`VALID`).

Дополнительно по gap-review и следующему этапу production P:
- После повторной сверки `nalog target.docx`, тестового Excel и XSD подготовлена отдельная сводка: `nalog docs/сводка по непередаваемой бизнес-информации и блокам КС-2.md`.
- Подтверждено, что формат допускает дополнительные табличные блоки КС-2:
  - множественные `НаимИСт`;
  - множественные `Раздел`;
  - множественные `СвВидРаб`;
  - множественные `РасшифРес`.
- Одновременно зафиксировано ограничение формата: в одном `...P`-файле не поддерживаются несколько независимых шапок акта, потому что `СвАктСдПр`, `СвПродПер`, `ВсегоАктОтч`, `НастрФормДок` остаются одиночными.

Дополнительно по приоритету 1 для production P:
- В `СвПродПер/СвПер` внедрены атрибуты периода документа:
  - `НачПерВДок`
  - `ОконПерВДок`
- Значения периода теперь вычисляются по диапазону данных `ks2Sheets[*].document.periodFrom/periodTo` с fallback на `ks3.document.periodFrom/periodTo`.
- В `СвПродПер/СвПер/@СодОпер` теперь передается живой текст операции из `common.operationType`, а при его отсутствии сохраняется fallback `О ПРИЕМКЕ ВЫПОЛНЕННЫХ РАБОТ`.
- `СвАктСдПр/ОснСдачи` переведен на множественное заполнение: в XML попадают все уникальные основания из листов КС-2, а не только первое.
- В production-full режиме `Документ/НаимИСт` теперь формируется множественно: по одному блоку на каждый лист `ks2Sheets`, при сохранении сквозной нумерации строк и разделов.
- После этих изменений базовый файл `output/small-sample-full.xml` по-прежнему проходит XSD (`VALID`).
- Дополнительно собран искусственный multi-KS2 тест `output/multi-ks2-test-full.xml`: он проходит XSD (`VALID`) и подтверждает, что схема принимает `НаимИСт x2` и `ОснСдачи x2` в одном `...P`-файле.

Архитектурное решение на текущий этап:
- Зафиксирован рабочий вариант комплекта: **один `P`-файл + один `Z`-файл**.
- Дальнейшая разработка и проверка ведутся только для этого сценария.
- Сценарии `несколько P на один Z` и `отдельная пара P+Z на каждый КС-2` пока выводятся за рамки текущего этапа.
- Если в исходных данных несколько КС-2, на текущем этапе они должны агрегироваться внутрь одного `P`, на который затем формируется один `Z`.

Дополнительно по максимальному сохранению Excel-данных в одном `P`:
- Для каждого блока `НаимИСт`, соответствующего отдельному листу КС-2, в первый `СвВидРаб` добавляется `ИнфПолФХЖ2` со служебными метаданными листа.
- Через `ИнфПолФХЖ2/ТекстИнф` теперь сохраняются:
  - `ks2.sheetIndex`
  - `ks2.sheetTitle`
  - `ks2.documentNumber`
  - `ks2.documentDate`
  - `ks2.periodFrom`
  - `ks2.periodTo`
  - `ks2.basis`
  - `ks2.vatRate`
- Это решение позволяет при агрегации нескольких КС-2 в один `P` не терять шапочные данные отдельных листов, даже если сама верхнеуровневая модель `1110335` не поддерживает несколько независимых `СвАктСдПр`.
- После добавления `ИнфПолФХЖ2` multi-KS2 файл `output/multi-ks2-test-full.xml` по-прежнему проходит XSD (`VALID`).

Дополнительно по подписанту подрядчика в `P`:
- В `ПодписантПодр/Подписант` добавлена поддержка доверенности для статусов:
  - `СтатПодп=2` -> `СвДовер`
  - `СтатПодп=3` -> `СвДоверБум`
- Для `СтатПодп=2` поддержаны поля:
  - `signerPowerId` (используется как 36-символьный идентификатор доверенности)
  - `signerPowerDate`
  - `signerPowerInternalNumber`
  - `signerPowerRegistrationDate`
  - `signerPowerSystemMark`
- Для `СтатПодп=3` поддержаны поля:
  - `signerPaperPowerDate`
  - `signerPaperPowerInternalNumber`
  - `signerPaperPowerIdentity`
  - `signerPaperPowerFio`
- Дополнительно в `ПодписантПодр/Подписант` поддержан `ИдСистХран` через `signatureStorageId`.
- Разбор ФИО подрядческого подписанта приведен к той же логике, что и в `Z`: формат `Дылюк А.` теперь сохраняется как `Фамилия="Дылюк" Имя="А"`.
- Проверены три файла:
  - `output/small-sample-full.xml` -> `VALID`
  - `output/p-signer-power-ef.xml` -> `VALID`
  - `output/p-signer-power-paper.xml` -> `VALID`


2026-03-20

Дополнительно по full cumulative для подрядческого файла `P`:
- Принято отдельное проектное решение: для `P` больше не используем режимы `0/2`; рабочий режим зафиксирован как `ПрНакИтог=1` (`full cumulative`).
- Этот режим трактуем как развернутый аналог КС-3 из Excel: в `P` должны передаваться не только отчетные суммы, но и показатели `с начала` там, где формат это допускает.
- В `scripts/generate_xml_from_export.py` режим `ПрНакИтог` переведен на фиксированное значение `1`.
- В генератор `P` добавлено формирование `ВсегоАктСНач` на основе `ks3.totals.fromStart` (с fallback на отчетные итоги, если накопительные данные еще не пришли в модель).
- В `ВсегоАктОтч` и `ВсегоАктСНач` теперь передаются both base+VAT totals: `СтТовБезНДСВсего`, `СтТовУчНалВсего`, `СумНалВсего`, `СумПоСтавке`.
- В full-profile для `Раздел` добавлены cumulative-поля:
  - `СтБезНДСРаздСНач`
  - `СтСНДСРаздСНач`
  - `СумНалРаздСНач` (если НДС считается не только в итоговой строке).
- В full-profile для `СвВидРаб` добавлены cumulative-поля:
  - `СтСНачСтрБезНДС`
  - `КолСНач`.
- Генератор `P` теперь пытается брать накопительные значения из экспортной модели напрямую (`fromStart` / `...FromStart` / `cumulative...`).
- Дополнительно добавен safe-bridge от КС-3:
  - на уровне документа используются `ks3.totals.*`;
  - на уровне листа используются `ks3.rows[*]` и они сохраняются в служебных метаданных `ИнфПолФХЖ2` как `ks3.sheetFromStart` / `ks3.sheetForPeriod`;
  - для раздела / строки без явных cumulative-полей пока используется безопасный fallback на отчетное значение, а в однозначном кейсе `1 лист -> 1 раздел` cumulative поднимается из строки КС-3 листа.
- Это дает первый рабочий шаг к развернутому `P`, не выдумывая неподтвержденную детализацию там, где исходная форма пока не хранит накопительный разрез по каждой строке.
- Для UI/sample режим также синхронизирован на `cumulativeMode=1`:
  - `data/sample-data.json`
  - `saved-forms/small-sample.json`
  - `variants/modern-light/data/sample-data.json`
  - дефолт в `prepareState()`.
- Проверка после внедрения:
  - `output/small-sample-full-cumulative.xml` -> `VALID`
  - `output/multi-ks2-test-full-cumulative.xml` -> `VALID`.
- Следующий подэтап по `P`: дотянуть уже не только итоговый cumulative-слой, но и более точный источник накопительных значений по разделам/строкам из формы/экспорта, чтобы уйти от fallback на отчетные суммы там, где Excel реально дает отдельный накопительный разрез.
