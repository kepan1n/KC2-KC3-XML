# Закрытие ошибок XSD для `пример xml 1110335.xml`

## Контекст

- До исправлений: **8 ошибок XSD** (см. предыдущий прогон)
- После исправлений: **0 ошибок, PASS**
- Актуальный отчет: `nalog docs/пример xml 1110335 - отчет xsd.md`

## Как закрыты ошибки

| № | Ошибка (как была) | Причина | Что изменил | Результат |
|---|---|---|---|---|
| 1 | `Element 'ИННФЛ': This element is not expected.` | В `ИдРекСостТип` используется `choice`, а в XML одновременно были `ИННЮЛ` + `ИННФЛ` + другие ветки | Удалил конфликтующие ветки в `ОснДовОргСост/ИдРекСост`: оставил один вариант идентификации (через `ИННЮЛ`) | Ошибка ушла |
| 2 | `Element 'СвИнНеУч': This element is not expected.` | В `ИдСв` тоже `choice`; в `СвЗак/ИдСв` одновременно стояли `СвЮЛУч` и `СвИнНеУч` | Удалил `СвИнНеУч` в ветке `СвЗак`, оставил `СвЮЛУч` | Ошибка ушла |
| 3 | `Element 'АдрИнф': This element is not expected.` | В `АдресТип` (`МестВыпРаб`) `choice`: нельзя одновременно `АдрРФ`, `АдрИнф`, `КодГАР` | Оставил только одну адресную ветку (`АдрРФ`), удалил `АдрИнф` и `КодГАР` | Ошибка ушла |
| 4 | `Element 'ИзмСмет', attribute 'КодСмет': 'Пример' is not a valid value...` | `КодСмет` должен быть целым числом (по XSD), а было текстовое значение | Принудительно поставил `КодСмет="1"` | Ошибка ушла |
| 5 | `Element 'ИзмСмет': Missing child element(s). Expected is ( ИдДопСогл ).` | Для `ИзмСмет` обязателен дочерний `ИдДопСогл` | Добавил `ИдДопСогл/ТипИдДок` с `НаимДок`, `НомерДок`, `ДатаДок` | Ошибка ушла |
| 6 | `Element 'УменьшКол': This element is not expected. Expected is ( ПрДокИзмСНачСтр ).` | В `ИзмененияТип` два последовательных `choice`; были одновременно несколько взаимоисключающих элементов | В `ОшибПрПер` оставил только допустимый набор (`УвелДен + УвелКол`), удалил `УменьшКол` и `НетИзмКол` | Ошибка ушла |
| 7 | `Element 'ВидУдерж': This element is not expected. Expected is one of ( ИнВидТреб, ИнВидУдерж, ДокПодтСумУд ).` | В `СумТребУдержТип` первый `choice`: нельзя одновременно `ВидТреб` и `ВидУдерж` | Удалил `ВидТреб`, оставил `ВидУдерж="31"` | Ошибка ушла |
| 8 | `Element 'СумПоСтавке': Missing child element(s). Expected is one of ( СумНДС, НеобНДС, НДСНалАг ).` | Внутри `СумПоСтавке` отсутствовал обязательный элемент из `choice` | Добавил `СумНДС` и зафиксировал `НалСт="20%"`, `НалБаза` | Ошибка ушла |

## Где именно внесены правки

- Скрипт генерации: `scripts/generate_mandatory_xml_sample.py`
  - добавлены `forced`-переопределения для ограниченных полей;
  - добавен блок удаления конфликтующих узлов по `choice`/`sequence`;
  - добавлены обязательные дочерние элементы для `ИзмСмет` и `СумПоСтавке`.

## Финальная проверка

- `python3 scripts/generate_mandatory_xml_sample.py`
- Итог: **XSD errors = 0**
